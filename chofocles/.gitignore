# Vendor-copy de saycu-theme hecho por _scripts/deploy-panel.sh.
# La fuente real es /Volumes/THUND/proyectos/saycu/saycu-theme/.
saycu-theme/

# Builds y dependencias
panel/dist/
panel/node_modules/
api/node_modules/
ingestor/__pycache__/

# .env reales (los .example sí van al repo).
# .env-dev y .env-prod viven en el servidor correspondiente; el bootstrap
# copia el adecuado a .env según /etc/hosts (marca DESARROLLO).
.env
.env-dev
.env-prod
